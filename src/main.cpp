#include <iostream>
#include "utils.cpp"
using namespace std;

int main() {
    int x = 10;
    int y = 5;
    cout << add(x, y) << "\n";
    cout << subtract(x, y) << "\n";
    cout << multiply(x, y);
    return 0;
}

